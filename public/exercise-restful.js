/**
 * EXERCISE
 * Chaining observables
 */

const apiUrl = 'https://jsonplaceholder.typicode.com/posts';

/**
 * Create an observable from a promise that calls `fetch(apiUrl)`
 */
const apiRequest = undefined;

/**
 * Take the API request and get its JSON data
 */
const apiJson = undefined;

/**
 * Is the API call not being made? Then consider some of these:
 *
 * - What could be the cause to the API call not being made?
 * - What happens if you peek inside the observable? Why?
 */
