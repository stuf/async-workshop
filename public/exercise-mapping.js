/**
 * EXERCISE
 * Mapping values and errors
 *
 * NOTE
 * There exists two Observables for you to use as sources in this exercise.
 * `tick` is an observable emitting the current time every second
 * `tickError` is an observable emitting the current time every second as error
 */

// DO NOT TOUCH
// ------------

const setOriginalTime = setIntoElement('original');
const setModifiedTime = setIntoElement('modified');

const setOriginalErrorTime = setIntoElement('original-error');
const setModifiedErrorTime = setIntoElement('modified-error');

// BEGIN EXERCISE
// --------------

/**
 * Use the `tick` observable to print out the current time in a format you choose.
 * You can use `date.toTimeString()`, or if you want to get creative,
 * you can use existing `Date` methods like `getSeconds` and `getHours`
 */
const modified = undefined;

/**
 * Use the `tickError` observable to print out the current time, like above, but
 * add the string "ERROR" in front of it.
 *
 * Remember that you can join two strings together with `+`: "a string" + "another string"
 */
const modifiedError = undefined;

/**
 * Finally, `observe` every
 */
// tick.observe();
// modified.observe();
// tickError.observe();
// modifiedError.observe();
